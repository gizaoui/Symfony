{% extends 'base.html.twig' %}

{% block title %}Mise à jour
{% endblock %}

{% block body %}

	<div class="example-wrapper">
		<h1>{{ recipeData.title }}</h1>
		{{ form(recipeForm)}}
	</div>

{% endblock %}
